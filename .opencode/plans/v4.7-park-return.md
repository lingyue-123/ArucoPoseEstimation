# v4.7 停靠-返回 (STATE_11B) 实施计划

## 目标
基于 v4.6 新建 `scripts/oneshot_move_crobot_insert_auto_brightness_v4.7.py`，在 STATE_11(力控插枪) 与 STATE_12(力控拔枪) 之间插入新状态 STATE_11B：开夹爪→沿法兰z退200mm→记pose1→去FINALL_POINT→等12s→回pose1→沿法兰z插200mm→关夹爪。所有机械臂运动用关节运动。删除 STATE_12 中原有的 sleep(12)。

## 已确认决策
- 夹爪: 开=`flow.gripper.open()`(pos0，含等待status=1)；关=`set_position(32)`+speed30+force100(同STATE_8，等待status∈{1,2})
- 删除 STATE_12 里的 `time.sleep(12)` 与 "正在充电" 日志
- pose1 同时记录关节角(精确返回)+笛卡尔TCP(算+200插入目标)
- 单个新状态 STATE_11B_PARK_RETURN=110
- 速度: 200mm z向=30；FINALL_POINT=60

## v4.7 文件已通过 cp 创建
`cp scripts/oneshot_move_crobot_insert_auto_brightness_v4.6.py scripts/oneshot_move_crobot_insert_auto_brightness_v4.7.py`（已完成）

## 待应用的编辑（plan mode 关闭后执行）

### 编辑1: docstring 标题 (行3)
`Full Auto Insert/Extract Pipeline (v4.6)` → `(v4.7)`

### 编辑2: docstring 步数 (行6)
`自动按序执行 17 步状态机` → `自动按序执行 18 步状态机`

### 编辑3: docstring 状态机流程 (行20-21)
```
    STATE_11  → 力控插枪
    STATE_11B → 开夹爪+沿法兰z退200mm+记pose1+去FINALL_POINT → 等12s → 回pose1+沿法兰z插200mm+关夹爪 (全部关节运动)
    STATE_12  → 力控拔枪
```

### 编辑4: 用法示例 (行41-42)
两处 `v4.6.py` → `v4.7.py`

### 编辑5: AutoState 枚举 (行119-120之间插入)
```python
    STATE_11_FORCE_IN = 11
    STATE_11B_PARK_RETURN = 110
    STATE_12_FORCE_OUT = 12
```

### 编辑6: argparse description (行464)
`'充电枪全流程自动化 v4.6'` → `'充电枪全流程自动化 v4.7'`

### 编辑7: 替换 step_11 返回 + 插入 _step_11b + 修改 step_12 开头
oldString (行1257-1267):
```python
        logger.info("=== STATE_11 完成 ===")
        return AutoState.STATE_12_FORCE_OUT

    def _step_12_force_out():
        nonlocal moving
        if _check_abort(): return AutoState.IDLE
        timer.set_step("STATE_12")
        logger.info("=== STATE_12: 力控拔枪 ===")
        logger.info("=== 正在充电 ===")
        time.sleep(12)
        if robot_connected:
```
newString:
```python
        logger.info("=== STATE_11 完成 ===")
        return AutoState.STATE_11B_PARK_RETURN

    def _step_11b_park_return():
        nonlocal moving
        if _check_abort(): return AutoState.IDLE
        timer.set_step("STATE_11B")
        logger.info("=== STATE_11B: 开夹爪→退200mm→记pose1→去FINALL_POINT→等12s→回pose1→插200mm→关夹爪 ===")
        if robot_connected:
            # 力控插枪后当前 TCP (tool 0 = 法兰)
            force_in_tcp = get_tcp_pose_in_tool_mm(
                robot, robot_cfg, INSERT_TOOL_ID, label=f"tool {INSERT_TOOL_ID}(insert)")
            if force_in_tcp is None:
                logger.error("STATE_11B 失败: 无法读取 TCP")
                return AutoState.IDLE
            # 1) 开夹爪 (释放枪)
            with timer.segment("开夹爪"):
                flow.gripper.open()
                timeout_s = 5.0
                t0 = time.time()
                while True:
                    status = flow.gripper.get_grip_status()
                    if status in (1,):
                        logger.info("夹爪打开完成 (status=%d)", status)
                        break
                    if time.time() - t0 > timeout_s:
                        logger.warning("夹爪打开超时 %.1fs (status=%s)", timeout_s, status)
                        break
                    time.sleep(0.1)
            moving = True
            # 2) 沿法兰 z 轴退出 200 mm (关节运动)
            exit_pose = offset_pose_along_tool_axis(force_in_tcp, -200.0, axis='z')
            cur_joint = robot.get_joint_pose()
            exit_joint = robot.inverse_kinematics(target_pose=exit_pose, initial_joints=cur_joint)
            logger.info("沿法兰z退出200mm: 目标 X=%.2f Y=%.2f Z=%.2f Rx=%.2f Ry=%.2f Rz=%.2f", *exit_pose)
            with timer.segment("沿法兰z退出200mm"):
                robot.move_by_joint_list(joints=[exit_joint], speeds=[30])
            # 3) 记录 pose1 (关节角 + 笛卡尔)
            pose1_joint = robot.get_joint_pose()
            pose1_cart = get_tcp_pose_in_tool_mm(
                robot, robot_cfg, INSERT_TOOL_ID, label=f"tool {INSERT_TOOL_ID}(insert)")
            logger.info("pose1 已记录: joint=%s", pose1_joint)
            if pose1_cart is not None:
                logger.info("  pose1 tcp: X=%.2f Y=%.2f Z=%.2f Rx=%.2f Ry=%.2f Rz=%.2f", *pose1_cart)
            # 4) 移动至 FINALL_POINT (关节运动)
            final_joint = flow.CFG["FINALL_POINT"]
            logger.info("移动至 FINALL_POINT: %s", final_joint)
            with timer.segment("移动至FINALL_POINT"):
                robot.move_by_joint_list(joints=[final_joint], speeds=[60])
            moving = False
            # 5) 充电等待 12 秒
            logger.info("=== 充电等待 12s ===")
            time.sleep(12)
            moving = True
            # 6) 反向：返回 pose1 (关节运动，用记录的关节角直连)
            logger.info("返回 pose1")
            with timer.segment("返回pose1"):
                robot.move_by_joint_list(joints=[pose1_joint], speeds=[60])
            # 7) 沿法兰 z 轴插入 200 mm (关节运动)
            if pose1_cart is None:
                logger.error("STATE_11B 失败: pose1 笛卡尔为空，无法计算插入目标")
                return AutoState.IDLE
            insert_pose = offset_pose_along_tool_axis(pose1_cart, 200.0, axis='z')
            cur_joint = robot.get_joint_pose()
            insert_joint = robot.inverse_kinematics(target_pose=insert_pose, initial_joints=cur_joint)
            logger.info("沿法兰z插入200mm: 目标 X=%.2f Y=%.2f Z=%.2f Rx=%.2f Ry=%.2f Rz=%.2f", *insert_pose)
            with timer.segment("沿法兰z插入200mm"):
                robot.move_by_joint_list(joints=[insert_joint], speeds=[30])
            moving = False
            # 8) 关夹爪 (重新夹住枪，同 STATE_8)
            with timer.segment("关夹爪"):
                flow.gripper.set_speed(30)
                flow.gripper.set_force(100)
                flow.gripper.set_position(32)
                timeout_s = 5.0
                t0 = time.time()
                while True:
                    status = flow.gripper.get_grip_status()
                    if status in (1, 2):
                        logger.info("夹爪闭合完成 (status=%d)", status)
                        break
                    if time.time() - t0 > timeout_s:
                        logger.warning("夹爪闭合超时 %.1fs (status=%s)", timeout_s, status)
                        break
                    time.sleep(0.1)
        else:
            logger.info("[DRY RUN] STATE_11B skipped")
        logger.info("=== STATE_11B 完成 ===")
        return AutoState.STATE_12_FORCE_OUT

    def _step_12_force_out():
        nonlocal moving
        if _check_abort(): return AutoState.IDLE
        timer.set_step("STATE_12")
        logger.info("=== STATE_12: 力控拔枪 ===")
        if robot_connected:
```

### 编辑8: _STEP_DISPATCH (行1349-1350之间插入)
```python
        AutoState.STATE_11_FORCE_IN: _step_11_force_in,
        AutoState.STATE_11B_PARK_RETURN: _step_11b_park_return,
        AutoState.STATE_12_FORCE_OUT: _step_12_force_out,
```

### 编辑9: 状态栏显示 (行1535)
`f"V4.6 AUTO{lr_flag}...` → `f"V4.7 AUTO{lr_flag}...`

## 依赖确认（均已满足）
- `offset_pose_along_tool_axis` 已导入 (行75)
- `get_tcp_pose_in_tool_mm` 本地定义 (行438)
- `flow.CFG` 已加载 (行636-637)，`flow.CFG["FINALL_POINT"]` 为关节角列表
- `flow.gripper` 已初始化 (行635)；`open()`=pos0，`set_position(32)`=抓枪
- `robot.move_by_joint_list / inverse_kinematics / get_joint_pose` 均在它处使用
- 符号约定: `offset_pose_along_tool_axis(pose, -200, 'z')`=退; `+200`=插 (同 step_9/step_8)
